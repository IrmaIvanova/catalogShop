{
  "name": "catalogshop",
  "version": "1.0.0",
  "description": "Каталог курсов с поиском, фильтрацией и Load More (Vanilla JS)",
  "scripts": {
    "build:css": "sass src/scss:dist/css --style compressed",
    "watch:css": "sass --watch src/scss:dist/css",
    "copy:html": "cp src/index.html dist/index.html",
    "copy:js": "cp -r src/js dist/",
    "copy:images": "cp -r src/images dist/",
    "build": "npm run build:css && npm run copy:html && npm run copy:js && npm run copy:images",
    "dev": "npm run build && npx live-server dist",
    "start": "npm run dev"
  },
  "devDependencies": {
    "sass": "^1.77.0",
    "live-server": "^1.2.2"
  },
  "keywords": ["catalog", "vanilla-js", "scss", "bem"],
  "author": "Irma Ivanova",
  "license": "MIT"
}